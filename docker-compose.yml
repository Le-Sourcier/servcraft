services:
  servcraft:
    build:
      context: .
    image: servcraft:latest
    container_name: servcraft
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
    ports:
      - "3200:3000"
    restart: unless-stopped
